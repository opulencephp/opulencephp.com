language: php

php:
  - 7.0

before_script:
  - composer self-update
  - composer install
  - composer dump-autoload -o
  - chmod -R 755 resources
  - chmod -R 755 tmp
  - php apex api
  - php apex docs

script: vendor/bin/phpunit --verbose

notifications:
  email:
    - dbyoung2@gmail.com

sudo: false

matrix:
  fast_finish: true